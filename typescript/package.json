{
  "name": "@vsaga/workspace",
  "version": "0.0.0",
  "private": true,
  "description": "vSaga TypeScript SDK: participate in vSaga sagas from Node backends",
  "packageManager": "npm@11.11.0",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "samples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc -b --pretty && tsc -p tsconfig.test.json --pretty",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{ts,js,mjs,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,js,mjs,json,md}\"",
    "clean": "rimraf packages/*/dist packages/*/*.tsbuildinfo samples/*/dist samples/*/*.tsbuildinfo"
  },
  "//": "esbuild override: tsup and vitest both pin ^0.27, inside GHSA-g7r4-m6w7-qqqr's range (arbitrary file read via esbuild's dev server on Windows). Dev-only, and nothing here runs that dev server, so it was never exploitable in this repo -- but a permanently non-empty `npm audit` is one nobody reads. Drop this once tsup and vitest allow 0.28 themselves.",
  "overrides": {
    "esbuild": "^0.28.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^22.19.0",
    "eslint": "^9.39.1",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "tsup": "^8.5.0",
    "typescript": "~5.9.2",
    "typescript-eslint": "^8.46.1",
    "vitest": "^4.1.11"
  }
}
