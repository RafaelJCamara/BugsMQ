<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\BugsMQ.Abstractions\BugsMQ.Abstractions.csproj" />
    <ProjectReference Include="..\BugsMQ.Transport.Common\BugsMQ.Transport.Common.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <!-- Not centrally versioned: Directory.Packages.props is frozen for this track (see task notes:
         do not edit the shared file, use VersionOverride per-project instead), so these packages are
         pinned here directly. 10.7.0 is the latest stable release on NuGet as of this work (confirmed
         2026-08-26); RMQ.Async is the async-native gateway package built on RabbitMQ.Client v7, matching
         this repo's already-centrally-pinned RabbitMQ.Client 7.2.2. -->
    <PackageReference Include="Paramore.Brighter" VersionOverride="10.7.0" />
    <PackageReference Include="Paramore.Brighter.MessagingGateway.RMQ.Async" VersionOverride="10.7.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
