<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <!-- Brings in BugsMQ.Persistence.EFCore (BugsMqDbContext/SagaInstanceEntity/SagaTimeoutEntity)
         transitively, same as BugsMQ.Dashboard.Api — referenced directly rather than the plain
         BugsMQ.Persistence.EFCore project purely for consistency with that pattern, even though this
         tool never touches migrations. -->
    <ProjectReference Include="..\..\src\BugsMQ.Persistence.EFCore.Postgres\BugsMQ.Persistence.EFCore.Postgres.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
  </ItemGroup>

</Project>
