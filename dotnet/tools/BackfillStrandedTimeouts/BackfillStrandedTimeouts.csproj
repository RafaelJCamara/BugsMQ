<Project Sdk="Microsoft.NET.Sdk">

  <!-- A standalone CLI tool, not a library — opt out of the repo-wide IsPackable=true default. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Brings in VSaga.Persistence.EFCore (VSagaDbContext/SagaInstanceEntity/SagaTimeoutEntity)
         transitively, same as VSaga.Dashboard.Api — referenced directly rather than the plain
         VSaga.Persistence.EFCore project purely for consistency with that pattern, even though this
         tool never touches migrations. -->
    <ProjectReference Include="..\..\src\VSaga.Persistence.EFCore.Postgres\VSaga.Persistence.EFCore.Postgres.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
  </ItemGroup>

</Project>
