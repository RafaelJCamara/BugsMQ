<Project Sdk="Microsoft.NET.Sdk">

  <!-- A sample app's contracts, not a library — opt out of the repo-wide IsPackable=true default. -->
  <PropertyGroup>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

</Project>
