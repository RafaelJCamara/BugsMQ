<Project Sdk="Microsoft.NET.Sdk">

  <!-- Package-specific description for nuget.org search results, overriding Directory.Build.props's
       shared fallback (MSBuild evaluates that file first, so this later value wins). -->
  <PropertyGroup>
    <Description>Core saga abstractions for vSaga: SagaState, ISagaContext, message envelopes, and the transport/persistence interfaces every other vSaga package builds on.</Description>
  </PropertyGroup>

</Project>
